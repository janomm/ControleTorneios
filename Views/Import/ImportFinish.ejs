<%- include('../partials/header.ejs') %>
<%- include('../partials/navbarUser.ejs') %>
<div class="container">
    <hr>
    <div class="card">
        <div class="card-header">
            <h2>Importação Log</h2>
        </div>
    </div>
    <hr>
    <div class="card">
        <div class="card-body">
            <% if(dados.importou) { %>
                Importou Corretamente
            <% } else { dados.validar.forEach(e => { %>
                <p><%= e.erro %></p>
            <% }) } %>
        </div>
    </div>
</div>

<%- include('../partials/footer.ejs') %>


