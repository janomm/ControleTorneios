<%- include('../partials/header.ejs') %>
<%- include('../partials/navbarUser.ejs') %>
<div class="container">
    <h2>Meus Rankings</h2>
    <hr>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Tipo Torneio</th>
                <th>Formato</th>
                <th>Data Início</th>
                <th>Data Final</th>
            </tr>
        </thead>
        <tbody>
            <% rankings.forEach(ranking => { %>
                <tr>
                    <td><%= ranking.nome %></td>
                    <td><%= ranking.TipoTorneioNome %></td>
                    <td><%= ranking.FormatoNome %></td>
                    <td><%= ranking.dtInicio %></td>
                    <td><%= ranking.dtFinal %></td>
                    <td>
                        <a href="/rankings/viewJogador/<%= ranking.id %> " class="btn btn-success ">Ver Ranking</a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
<%- include('../partials/footer.ejs') %>